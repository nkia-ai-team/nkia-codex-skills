interface:
  display_name: "Finish"
  short_description: "Post-merge wrap-up, evidence validation, and status roll-up"
  default_prompt: "Use $finish NKIAAI-305 after PR/MR merge to clean branches, register evidence, validate AC, and update Linear."
